# Cloudflare compatibility audit

Baseline audited: `ab99d56e9626e6cd731592dae8553c9758a0efa2`.

This is a source audit, not a migration design or a statement that any
Cloudflare service below is suitable. A **candidate/unverified** target must
pass its listed acceptance test before it is treated as a replacement.

## Accepted implementation overlay: `d5a8c6ee0`

The source matrix below preserves the traditional-mode baseline and the
unmigrated surface. The following rows record the granular Cloudflare
foundations committed between `62bb448e4` and `d5a8c6ee0`; they are not claims
of broad public wiring, remote deployment, or production equivalence.

| Committed foundation | Source and test evidence | Current boundary |
| --- | --- | --- |
| Offline migration and restore profile | `backend/internal/cloudflaremigration/toolkit.go`: `CanonicalizeRestore0018`, `UpgradeRestoreBundleTo0018`, `BuildRestoreSQLPlan0018`; `backend/cmd/cloudflare-migrate/main.go`; `toolkit_test.go` and `cloudflare-migrate/main_test.go` 0018 cases | Canonical migration/restore selection and reporting cover `0001-0018`, including the auth-session schema and pristine-state checks. This is offline deterministic evidence, not a remote restore. |
| Auth-session rotation and reuse | `backend/internal/cloudflarebridge/auth_sessions.go`; `deploy/cloudflare/src/auth-sessions.ts`; `auth_sessions_test.go`, `auth-sessions.test.ts`, and `backend/internal/service/auth_refresh_rotation_test.go` | Go + Worker bridge supports atomic rotation, family/user revocation, and consumed-token reuse detection. OAuth provider calls and provider wiring remain open. |
| Scheduler, rate limits, and lease cleanup | `deploy/cloudflare/src/scheduler-policy.ts`, `scheduler-runtime.ts`, `rate-limit.ts`, `lease.ts`; `scheduler-policy.test.ts`, `scheduler-runtime.test.ts`, `rate-limit.test.ts`, and `lease.test.ts` | Multi-account policy/runtime and account/user/API-key fixed-window limits are present with fenced leases, reverse compensation, alarms, cooldown/health evidence, and post-commit cleanup. This is a bounded control-plane foundation, not complete public scheduler/producer integration. |
| Pricing, reservation, settlement, and outbox conflicts | `deploy/cloudflare/src/pricing.ts`, `control-plane.ts`, `billing.ts`; `billing-reservations.test.ts`, `control-plane.test.ts`, and `queue.test.ts` | Admission pins an exact pricing card and E8 reservation; settlement/recovery preserves unknown usage; D1 outbox conflict payloads are durably audited and fail closed. This does not imply a complete subscription/balance/ledger product integration. |
| Background-job executor spine | `deploy/cloudflare/src/job-executors.ts`, `job-worker.ts`, `index.ts`; `job-runtime.test.ts`, `job-worker.test.ts`, `queue.test.ts`, and `index.test.ts` | Four bounded private executor route registrations and Queue/recovery semantics are committed. Producers and concrete OAuth/payment/subscription/email job implementations are not wired. |
| Subscription private control plane | `deploy/cloudflare/src/subscription-control.ts` and `subscription-runtime.ts`; `subscription-control.test.ts` and `subscription-runtime.test.ts` | The Worker private route table covers plan, assignment, windows, usage reservation, and expiry operations. No Go bridge, public route, or gateway admission wiring is committed. |
| Encrypted settings adapter | `deploy/cloudflare/src/settings-control.ts` and `settings-runtime.ts`; `backend/internal/cloudflarebridge/settings_repository.go`; `settings-control.test.ts`, `settings-runtime.test.ts`, and `settings_repository_test.go` | Private encrypted D1 settings plus the Go `service.SettingRepository` bridge are tested, with `set-multiple`/`mutate` bounded to 16 entries. Broad admin `UpdateSettings` is not wired; the auth-settings composition correction is pending outside this checkpoint. |
| Payment private adapter | `deploy/cloudflare/src/payment-control.ts` and `payment-runtime.ts`; `payment-control.test.ts` and `payment-runtime.test.ts` | Private order/refund/provider-event state-machine operations are tested. Provider calls, webhook ingress, and public Go wiring remain open. |
| Email private D1 adapter | `deploy/cloudflare/src/email-control.ts` and `email-runtime.ts`; `email-control.test.ts` and `email-runtime.test.ts` | Challenge and delivery-job state use independent token/delivery keyrings and D1 leases. No provider sending or producer is wired. |
| Private ingress and service test harness | `deploy/cloudflare/src/index.ts`: `RESERVED_INGRESS_NAMESPACES`, `routeIngress`, `routePrivateControlPlane`; `index.test.ts`; service cleanup commits `62b63000d` and `d5a8c6ee0` | `/internal/cloudflare` and `/v1/private` remain reserved from public ingress, including normalized encodings. Race-prone shared service test state was removed and Gin test mode is initialized once. |

## Stage C management evidence overlay

The branch now has a bounded D1-backed management surface in addition to the
stage B gateway slice. The API-key rebind, OpenAI API-key account CRUD, and
administrator balance adjustment/history have local composed-browser results;
TOTP/login/step-up and administrator role changes have local composed-HTTP
results. The other rows remain layer-level local evidence. None replaces the
pending broad-console, remote, real-upstream, or production gates.

| Feature slice | Implemented target | Local evidence | Status |
| --- | --- | --- | --- |
| Admin user create | Existing console API -> Cloudflare Go handler -> private Worker mutation -> D1 user and operation rows | Go HTTP/control-plane tests, workerd D1 tests, frontend retry tests, fresh migration, and full image build cover semantic replay across new IDs/bcrypt hashes, exact microUSD conversion, normalized email conflicts, group references, and private auth readback | Automated layers locally verified for ordinary `user` creation; admin creation, default balance/subscriptions, internationalized email, and a composed browser/runtime probe remain open |
| Admin user update | Field-level D1 patch with private credential readback; role-changing patches use the separate step-up-gated role operation | Tests cover password replacement, omitted-field preservation, unrelated concurrent balance preservation, conditional group validation, normalized email uniqueness, management-response credential rejection, role-operation replay/conflict, and final-admin protection | Locally composed-HTTP verified for role promotion/demotion and automated locally verified for email/password/profile/status/limits/groups; balance uses its dedicated audited endpoint |
| Admin user delete | One D1 batch tombstones the non-admin user and every live owned API key | Workerd tests cover successful tombstoning, private-auth absence, operation replay, and a zero-row admin guard that leaves keys untouched | Automated layers locally verified for non-admin users; admins remain protected and subscription/ledger cleanup is not implied |
| User TOTP and session step-up | Existing auth/TOTP APIs -> Cloudflare Go handler -> private Worker -> per-user `TOTPSecurityDO` plus D1 encrypted envelope/revision | Go route/control-plane tests and workerd tests cover password-gated setup/disable, one-time setup response, encrypted-at-rest state, login challenge consumption, five-attempt lockout, expiry/alarm cleanup, revision invalidation, and JWT-session-bound step-up; a fresh Wrangler/Container/D1 composition covers enable, 2FA login, replay rejection, step-up, disable, and direct-login restoration | Locally composed-HTTP verified. The existing console components were not driven in a browser for this lane; email verification, remote Cloudflare, and production remain open |
| Admin role promotion/demotion | Existing admin user PUT -> Cloudflare Go JWT/TOTP step-up boundary -> private Worker role mutation -> D1 user, idempotency, and immutable audit rows | Go, workerd, and frontend tests cover API-key rejection, exact session binding, strict payloads, same-request replay, changed-payload/password conflict, combined field/password patches, and concurrent final-admin protection; a fresh Wrangler/Container/D1 composition covers the complete public flow and persisted readback | Locally composed-HTTP verified. Direct admin creation/delete remain prohibited; console interaction, remote Cloudflare, real upstream, and production remain open |
| Admin balance adjustment/history | Existing console balance modal -> strict Cloudflare Go public API -> private Worker mutation/read -> guarded D1 balance projection, append-only ledger, and operation row | Go and workerd tests cover exact UTF-16/microusd boundaries, replay/conflict, stale and balance bounds, enum validation, immutable rows, and safe history projection; real Chromium covers add/refund, list refresh, modal history, ordering, notes, totals, and persisted D1 readback | Locally composed-browser verified for dedicated administrator add/subtract and immutable history; request pricing/reservation/settlement is separately committed, while broader subscription/balance integration, remote, and production equivalence remain open |
| Admin API-key group rebind | Existing console owner-key list and rebind routes -> strict Cloudflare Go handlers -> private Worker reads/mutation -> D1 | Go and workerd tests cover admin auth, exact request/response protocol, large IDs, key non-disclosure, active owner/key/group guards, same-group replay, exclusive access append, concurrent-state guards, and zero-row rollback; frontend tests cover Cloudflare filtering and traditional behavior; real Chromium plus persisted D1 readback covers the local composed interaction | Locally composed-browser verified for the upstream admin contract's active standard OpenAI group rebind; unsupported unbind, reset, quota/rate, and subscription-group extensions continue to fail closed |
| Admin account create | Existing console POST -> strict Cloudflare Go handler -> private Worker mutation -> D1 account, group-membership, and operation rows | Go route/bridge tests and workerd D1 tests cover admin auth, semantic replay across regenerated large candidate IDs, one ambiguous private 5xx retry with a stable operation ID, live compatible group references, complete safe readback, conflict mapping, and secret rejection; real Chromium covers the accepted modal and 200 create/list refresh without unsupported initialization probes | Locally composed-browser verified for OpenAI API-key accounts; ambiguous private-5xx retry remains automated, and remote/real-upstream acceptance is not claimed |
| Admin account update | Existing console PUT -> strict field patch -> one D1 batch for the account and optional memberships | Workerd tests cover omitted-envelope preservation, AES-GCM replacement, invalid-reference rollback, and zero-row primary-update rollback; Go tests cover strict response validation and public non-disclosure; real Chromium covers omitted-key preservation, credential replacement, status/scheduling toggles, and safe responses | Locally composed-browser verified for the bounded fields only; OAuth, refresh, test, and other account options fail closed |
| Admin account delete | Existing console DELETE -> disabled and unschedulable D1 tombstone | Go and workerd tests validate the tombstone response, exact replay, operation conflict, and post-delete admission rejection; real Chromium plus D1 readback covers row removal, tombstone/envelope/group state, operation rows, and foreign-key integrity | Locally composed-browser verified; no hard-delete or broader cleanup semantics are implied |
| Admin user browser create retry | Administrator-scoped `Idempotency-Key`; full payload fingerprint in memory and non-secret retry state in session storage | Frontend tests cover ambiguous retry reuse, password-change conflict isolation, definitive 4xx clearing, administrator separation, and absence of password/email in stored state | Locally verified at API-helper level; real embedded-console retry behavior remains open |
| Admin account browser create retry | Administrator-scoped `Idempotency-Key`; SHA-256 of the full credential-bearing payload remains memory-only while session storage contains only a credential-free fingerprint and key | Ten focused frontend API tests cover reconstructed payloads, reload recovery, changed payload/credential isolation while memory is live, 408 retention, definitive 4xx/success clearing, admin separation, exact decimal-string CRUD IDs, and absence of `api_key`/`base_url` in storage | Locally verified at API-helper/typecheck level; a real embedded-console ambiguous retry remains open |

Migration `0004_user_live_email_identity.sql` replaces the prior live-email
index with a unique `lower(trim(email))` identity. Existing D1 data must be
preflighted for duplicates before applying it; failure is intentional because
the migration must not choose an identity owner. The current write contract
admits ASCII email only, matching SQLite's built-in case folding used here.

## Stage B evidence overlay

The detailed rows below describe the full baseline surface and therefore remain
open unless stated otherwise. The first slice has now locally verified only
these bounded parts:

| Feature slice | Implemented target | Local evidence | Status |
| --- | --- | --- | --- |
| Cloudflare bootstrap | Separate pre-setup composition root; Worker routes to a distroless Go Container | Cloudflare mode starts and serves with no PostgreSQL/Redis; traditional full unit suite remains green | Locally verified for `/health` and Chat Completions slice only |
| API-key auth | D1 hash lookup with user/key/group status and balance checks | Active fixture succeeds; disabled/unknown states, user-owned key CRUD, bounded admin standard-group rebind, and public bridge forgery are covered in Go/workerd tests | Locally verified for gateway auth, user-owned key CRUD, and bounded admin group rebind; quota/rate/reset and broader invalidation contracts remain open |
| Account concurrency | Account-keyed SQLite DO with persisted fenced leases, lazy expiry, alarms, renew/release | 24 Worker tests plus two real local Containers competing at max concurrency 1; forced kill recovers to zero | Locally verified for account leases; committed user/key RPM, scheduler policy/runtime, cooldown, and recovery foundations are covered separately; broader scheduler producers and remote behavior remain open |
| Model mapping cache | D1-authoritative alias with bounded KV cache | KV miss, stale value and KV failure all resolve against D1 in tests | Locally verified for one alias path only |
| Completion/outbox/usage | D1 completion and outbox, Queue publication, idempotent usage projection and conflict audit | Ten duplicates, conflict and out-of-order cases in tests; real local Queue produces one usage row per completed request | Locally verified with committed exact pricing/reservation/settlement and conflict evidence; complete product-level subscription/balance/ledger integration remains open |
| Lifecycle | Container sleep/restart plus durable D1/DO state | Idle wake, successful lease renewal past TTL, SIGKILL pending state, lease expiry and restart all exercised locally | Locally verified for the slice; rolling update and performance remain open |

| Feature | Source evidence (repo-relative paths and symbols) | Current dependency / semantics | Cloudflare target candidate | Required acceptance test | Status |
| --- | --- | --- | --- | --- | --- |
| Bootstrap, configuration, and dependency injection | `backend/cmd/server/main.go`: `main`, `runMainServer`; `backend/cmd/server/wire.go`: `initializeApplication`, `provideCleanup`; `backend/cmd/server/wire_gen.go`: `initializeApplication` | Long-running Go HTTP server loads config, constructs its graph with Wire, and starts it before listening. Setup persists `config.yaml` below `DATA_DIR` (or `/app/data`) (`backend/internal/setup/setup.go`: `GetDataDir`, `GetConfigFilePath`). | Workers bindings/secrets plus D1 for durable settings and KV only for stale, rebuildable, non-sensitive configuration cache: **D1/DO redesign candidate — unverified**. A Container-lifecycle DO may create/route to a Go Container; it must not own business coordination. | Cold start with no local writable disk: resolve required settings from bindings/D1, tolerate an absent KV cache, and serve a protected request. Prove a Container-lifecycle DO only manages container lifecycle/routing. | Migration work remaining: bootstrap/config redesign. |
| PostgreSQL schema, Ent, and direct SQL | `backend/internal/repository/ent.go`: `InitEnt`; `backend/internal/repository/wire.go`: `ProvideEnt`, `ProvideSQLDB`; `backend/migrations/`; `backend/internal/repository/dashboard_aggregation_repo.go`: `NewDashboardAggregationRepository`; `backend/internal/repository/usage_cleanup_repo.go`: `NewUsageCleanupRepository` | `InitEnt` opens PostgreSQL, applies embedded SQL migrations, bootstraps security secrets, then exposes both Ent and `*sql.DB`. Multiple repositories intentionally use native SQL, transactions, PostgreSQL syntax, aggregation, and partition cleanup. | D1 for relational durable records plus per-entity business Durable Objects where serialization/coordination is required: **D1/DO redesign candidate — unverified**. No PostgreSQL or external database is a Cloudflare-mode target. | Translate every migration/query to D1-compatible schema/query behavior; run CRUD, aggregation, cleanup, and transactional/idempotency cases. Prove each cross-row serialization requirement is either atomic in D1 or moved to the named business DO. | Migration work remaining: Ent/PostgreSQL SQL cannot carry over unchanged. |
| Repository contracts and persistence boundary | `backend/internal/service/api_key_service.go`: `APIKeyRepository`, `APIKeyCache`; `backend/internal/service/account_service.go`: `AccountRepository`; `backend/internal/service/scheduler_cache.go`: `SchedulerCache`; `backend/internal/repository/wire.go`: `ProviderSet` | Service code generally depends on interfaces, but the production providers bind many interfaces to Ent/SQL or Redis implementations. This is a useful seam, not proof that alternatives already exist. | Reimplement contracts over D1, business Durable Objects, Queues, and KV only for stale rebuildable non-sensitive cache: **D1/DO redesign candidate — unverified**. | Compile and run API-key, account, scheduler, billing, and batch-image integration tests with replacement providers; no direct `*ent.Client`, `*sql.DB`, or `*redis.Client` dependency remains. | Migration work remaining: adapter and domain-boundary work. |
| Redis foundation | `backend/internal/repository/redis.go`: `InitRedis`; `backend/internal/repository/wire.go`: `ProvideRedis`; `backend/go.mod`: `github.com/redis/go-redis/v9` | A mandatory Redis client backs distributed locks, caches, rate limits, live statistics, queues, sorted-set leases, Lua scripts, Pub/Sub, and `TIME`-based decisions. | Split by semantics: D1 for durable facts/outbox, per-entity business DOs for strongly serialized coordination, Queues for durable asynchronous delivery, and KV only for stale rebuildable non-sensitive cache: **D1/DO redesign candidate — unverified**. | For every Redis key family, demonstrate the selected replacement's atomicity, expiry/recovery, ordering, and retry behavior under concurrent Workers. | Migration work remaining: Redis behavior must be decomposed, not substituted wholesale. |
| API-key authentication and invalidation | `backend/internal/service/api_key_service.go`: `APIKeyService`, `StartAuthCacheInvalidationSubscriber`; `backend/internal/repository/api_key_cache.go`: `GetAuthCache`, `SetAuthCache`, `PublishAuthCacheInvalidation`, `SubscribeAuthCacheInvalidation`; `backend/internal/service/auth_cache_invalidation_outbox.go`: `AuthCacheInvalidationWorker`; `backend/internal/repository/auth_cache_invalidation_outbox_repo.go`: `Claim` | Auth has process-local L1 positive/negative caches, Redis L2, Redis Pub/Sub invalidation, and a durable PostgreSQL outbox with leases, retry, and a second safety pass. | D1 auth/outbox records plus a business DO keyed by API-key hash for serialized auth revision/invalidation; Workers may keep isolate-local cache only behind that revision. KV is permitted only as stale rebuildable non-sensitive cache: **D1/DO redesign candidate — unverified**. | Revoke/update a key while concurrent requests hit independent Workers; stale L1/KV entries must fail the DO/D1 revision check, and delivery retry must not leave a key authorized. | Migration work remaining: replace Pub/Sub and cache coherence. |
| API-key quotas, request limits, and concurrency | `backend/internal/repository/api_key_cache.go`: `IncrementCreateAttemptCount`; `backend/internal/middleware/rate_limiter.go`: `NewRateLimiter`; `backend/internal/repository/concurrency_cache.go`: `AcquireAccountSlot`, `AcquireUserSlot`, `AcquireLiveLease`; `backend/internal/service/concurrency_service.go`: `ConcurrencyCache`; Cloudflare `rate-limit.ts`, `lease.ts` | Traditional Redis semantics remain the baseline. The Cloudflare path now has account/user/API-key fixed-window RPM DOs, account leases, reverse compensation, alarms, and fencing. | Cloudflare business DO foundation: **committed and locally tested for the bounded admission contract**. A Container-lifecycle DO must not own these limits. | Parallel requests from separate Workers enforce the global limit; expired leases recover after worker termination and cannot release another request's lease. | The bounded Cloudflare foundation is committed; broader public rate-limit integration, production measurements, and route coverage remain open. |
| Account scheduling, sticky selection, cooldown, and temporary unschedulability | `backend/internal/repository/scheduler_cache.go`: `schedulerCache`, `AcquireGroupLifecycleLease`; `backend/internal/service/scheduler_service.go`: `SchedulerService`; Cloudflare `scheduler-policy.ts`, `scheduler-runtime.ts`; `scheduler-policy.test.ts`, `scheduler-runtime.test.ts` | Traditional Redis behavior remains the baseline. Cloudflare builds source-labelled D1/DO snapshots, deterministic multi-account selection, freshness gates, cooldown/health evidence, and bounded cross-DO acquisition. | Cloudflare scheduler policy/runtime foundation: **committed and locally tested for the bounded admission contract**. | With concurrent requests and separate Workers, verify sticky affinity, limits, cooldown, health-failure trip/reset, and no double selection of a leased account. | The committed policy/runtime and admission race correction are covered; complete public scheduler producers, broader route integration, and remote behavior remain open. |
| OAuth account refresh and credential mutation | `backend/internal/service/token_refresh_service.go`: `TokenRefreshService.Start`, `TokenRefreshService.Stop`; `backend/internal/service/openai_oauth_service.go`: `OpenAIOAuthService`; `backend/internal/service/gemini_oauth_service.go`: `GeminiOAuthService`; `backend/internal/service/grok_oauth_service.go`: `GrokOAuthService` | A ticker and goroutines page accounts and refresh provider credentials under rate/concurrency gates. OAuth-related account data is persistent; active refresh coordination includes process-local state and shared dependencies. | Scheduled Worker invokes a business DO keyed by account/provider; the DO serializes refresh and D1 holds credential state: **D1/DO redesign candidate — unverified**. | Trigger the schedule twice concurrently and simulate a retryable upstream failure; each account has at-most-one effective mutation per lease and preserves the correct terminal state. | Migration work remaining: cron, durable state, and refresh serialization. |
| Usage recording, billing, pricing, balances, and subscriptions | `backend/internal/service/usage_record_worker_pool.go`: `UsageRecordWorkerPool`, `Submit`; `backend/internal/repository/usage_log_repo_insert.go`: `runCreateBatcher`, `batchInsertUsageLogs`; Cloudflare `pricing.ts`, `billing.ts`, `control-plane.ts` | Traditional in-process/PostgreSQL/Redis behavior remains the baseline. Cloudflare now pins versioned pricing, exact E8 reservation/settlement, unknown-usage recovery, durable outbox publication, and conflicting-payload evidence. The Worker subscription runtime is private-only. | Cloudflare D1/DO/Queue foundation: **committed and locally tested for the bounded admission/billing contract**. | Under queue pressure and consumer restart, reconcile gateway responses, recorded usage, balances, and entitlement against the selected delivery/duplicate policy. | Exact pricing/reservation/settlement and outbox conflict handling are committed; complete subscription, default balance, public billing, and production equivalence remain open. |
| Orders, payment callbacks, and expiry | `backend/internal/server/routes/payment.go`: `RegisterPaymentRoutes`; `backend/internal/service/payment_service.go`: `PaymentService`, `CreateOrder`; Cloudflare `payment-control.ts`, `payment-runtime.ts` | Traditional authenticated/public/provider routes remain Go/Ent-backed. Cloudflare has only the private D1 payment state-machine adapter and tests. | Private Worker payment adapter: **committed and locally tested; provider/webhook/public Go wiring unimplemented**. | Replay the same callback concurrently and out of order; exactly one fulfilment/balance mutation occurs, then the expiry schedule handles an unpaid order. | Keep provider calls, webhook ingress, public Go wiring, and full expiry/fulfilment integration open. |
| Batch/async image work and object storage | `backend/internal/repository/batch_image_queue.go`: `BatchImageQueue`, `Enqueue`; `backend/internal/service/batch_image_worker_runtime.go`: `BatchImageWorkerRuntime.Start`; `backend/internal/repository/image_storage_s3.go`: `S3ImageStorage`; `backend/internal/repository/wire.go`: `ProvideImageStorageFactory` | Batch jobs have PostgreSQL records, Redis queue/locks/delayed and active state, and long-running worker goroutines. Optional image objects use an S3-compatible client. | Queues plus a business DO keyed by image job and D1 metadata: **D1/DO redesign candidate — unverified**. Binary image persistence is a declared compatibility gap: minimal product-set adjustment (for example R2) requires user authorization/architecture decision. | Submit a job and force consumer retry/crash; prove idempotent settlement and task polling. Separately decide whether binary retention is required before testing object retrieval. | Migration work remaining; binary persistence is out of the approved product set. |
| Backup, restore, import/export-style data operations | `backend/internal/service/backup_service.go`: `BackupService.Start`, `StartBackup`, `StartRestore`, `CreateBackup`, `RestoreBackup`; `backend/internal/server/routes/admin.go`: data-management routes | Backup uses `robfig/cron`, goroutines, local temporary `.sql.gz` files, a database dumper/restorer, and S3-compatible object storage; its own comment says the S3 config supports R2. | D1 export/import and a scheduled Worker are a redesign candidate only: **D1/DO redesign candidate — unverified**. Durable archive/binary retention is a declared compatibility gap: minimal product-set adjustment (for example R2) requires user authorization/architecture decision. | Prove a D1-native export/import recovery workflow without local temporary files; decide whether durable downloadable archives are a product requirement before adding storage. | Migration work remaining; current dump/restore is not preserved. |
| Local files: pricing, plugins, configuration, debug output | `backend/internal/service/pricing_service.go`: `NewPricingService`, `Stop`, `modelPricingPath`; `backend/internal/service/plugin_package.go`: `pluginRootDir`; `backend/internal/service/plugin_manager.go`: `PluginManager`; `backend/internal/setup/setup.go`: `GetConfigFilePath`; `backend/internal/service/gateway_service.go`: debug body path handling | Pricing makes directories, reads/writes JSON and hash files, and runs a reload ticker. Plugin install/runtime creates local directories and sockets. Setup writes config locally; optional gateway debug output uses a configured directory. | D1 for durable pricing/settings and KV only for stale rebuildable non-sensitive pricing cache: **D1/DO redesign candidate — unverified**. Plugin packages, Unix sockets, and debug-body files are declared compatibility gaps; a minimal product-set adjustment (for example R2) requires user authorization/architecture decision. | Restart after a pricing update and prove correct D1/KV reconstruction. Decide whether plugins/debug-body retention are required before any product-set adjustment. | Migration work remaining; local filesystem/plugin runtime unavailable. |
| Email work | `backend/internal/service/email_queue_service.go`: `EmailQueueService`, `EnqueueVerifyCode`, `EnqueuePasswordReset`, `Stop`; Cloudflare `email-control.ts`, `email-runtime.ts` | Traditional email remains a buffered in-process queue. Cloudflare now has D1 challenge/delivery state, fenced delivery jobs, and independent token/delivery keyrings. | Private Worker email adapter: **committed and locally tested; provider sending and producer wiring unimplemented**. | Enqueue messages across a Worker restart and intentional consumer failure; validate the chosen delivery/duplicate policy and user-visible failure behavior. | Private state semantics are covered; provider delivery, producer, and user-visible email behavior remain open. |
| Other lifecycle/background tasks | `backend/cmd/server/wire_gen.go`: `provideCleanup`; `backend/internal/service/channel_monitor_runner.go`: `ChannelMonitorRunner`; `backend/internal/service/payment_order_expiry_service.go`: `PaymentOrderExpiryService`; `backend/internal/service/ops_aggregation_service.go`: `OpsAggregationService`; `backend/internal/service/openai_quota_auto_reset.go`: `OpenAIQuotaAutoResetService`; Cloudflare `job-executors.ts`, `job-worker.ts` | The traditional graph still stops broad ticker/cron/goroutine services. Cloudflare has a registered, bounded executor/Queue spine and scheduled recovery, but no concrete producer wiring. | Scheduled Workers dispatch to D1/DO-backed business work: **executor spine committed; service-by-service producers remain unverified**. | Inventory every `Start`/`Stop` service in `provideCleanup`; for each, prove one intended execution, idempotent replay, and recovery after a missed schedule. | OAuth, payment, subscription, email, and other background producers remain open. |
| HTTP health and graceful shutdown | `backend/internal/server/routes/common.go`: `RegisterCommonRoutes`; `backend/cmd/server/main.go`: `runMainServer`; `backend/cmd/server/wire.go`: `provideCleanup` | `/health` returns static `{"status":"ok"}` and does not check PostgreSQL/Redis. The server handles SIGINT/SIGTERM, gives HTTP shutdown five seconds, then invokes cleanup with a ten-second context. | Worker liveness/readiness endpoint; if Go Container traffic is retained, a Container-lifecycle DO may route/manage that lifecycle only: **D1/DO redesign candidate — unverified**. | Verify cheap liveness, explicit D1/Queue/business-DO readiness policy, streaming behavior during deployment replacement, and that no business lock is held by the Container-lifecycle DO. | Migration work remaining: lifecycle semantics differ. |
| Gateway/provider protocol surface | `backend/internal/server/routes/gateway.go`: `RegisterGatewayRoutes`; `backend/internal/service/platform.go`: platform constants; `backend/internal/handler/endpoint.go`: `EndpointFromPath` | Source registers API-key-authenticated OpenAI-compatible paths (chat completions, responses, embeddings, images, async images, models, live/WebSocket), Anthropic-compatible messages/count-tokens, Gemini `v1beta`, Antigravity-specific routes, and OpenAI/Codex aliases. It also exposes Grok-only video, voice, realtime, and search endpoints under platform guards. | Worker HTTP streaming/WebSocket proxy layer: **candidate/unverified**. | Contract-test every registered supported route with streaming and WebSocket cases, authentication, platform guard failures, client disconnects, and request/response size limits. | Surface is large; runtime compatibility unverified. |

## Audit gaps / next probes

- Admin account CRUD has a bounded Cloudflare-mode implementation for OpenAI
  API-key accounts only. It uses D1 atomic account/group writes and does not
  introduce a business Durable Object. The traditional PostgreSQL/Redis route
  graph remains the implementation for every other account mode and operation.

- This audit identifies source dependencies but does not measure Cloudflare product limits, pricing, egress, D1/DO behavior, or WebSocket/streaming behavior. Those need current product documentation and a small non-production proof.
- `backend/cmd/server/wire_gen.go` is the authoritative assembled lifecycle graph, but each background service's cadence, idempotency contract, and leader-election behavior still need an individual code-level extraction before assigning a scheduled trigger or queue consumer.
- PostgreSQL migrations and direct SQL establish a redesign burden, not an external-database escape hatch. Each migration/query must be classified as D1-compatible, replaced by a business DO serialization path, or removed from the Cloudflare-mode product contract.
- “Business DO” in this document means a Durable Object whose identity follows a domain entity such as an API key, account, scheduling group, billable principal, or job. It owns correctness-critical serialization. “Container-lifecycle DO” means an object used only to create, locate, and route to a Go Container; it must not own those business locks, counters, or scheduler decisions.
- Plugin process/socket behavior, full database dump/restore, and durable binary/archive retention are particularly unsuitable for the requested Worker/D1/DO/KV/Queues/Container product set. The source proves the dependency; adding a minimal product-set adjustment (for example R2) requires user authorization/architecture decision.
- The route table establishes supported entrypoints, not end-to-end upstream compatibility. Streaming, WebSockets, request-body limits, provider OAuth redirect/callback behavior, and payment-provider callback IP/signature requirements remain probes.

# Administrator balance adjustment (Cloudflare mode)

The existing POST /api/v1/admin/users/:id/balance and
GET /api/v1/admin/users/:id/balance-history public shapes remain in Cloudflare
mode. Their established administrator authentication and audit semantics stay
at the Go public API; the private Worker receives only actor and target IDs,
operation, exact microusd amount, reason, and idempotency operation ID. It does
not receive credentials or TOTP material. Traditional mode remains on its
original Go service/repository path.

The local composed-browser gate added 1.25 and subtracted 0.25 from a 1.00
fixture balance. The refreshed list showed 2.00, and the history modal showed
the newest -0.25 row before +1.25, both notes, current balance 2.00, and total
recharged 1.25. Persisted D1 readback matched both before/after transitions,
the two idempotency rows, immutable-ledger triggers, and an empty foreign-key
check. Remote D1, real-upstream, and production equivalence are not claimed.

# TOTP and session-bound step-up (Cloudflare mode)

The existing setup, enable, disable, login-2FA, and step-up public contracts now
route through the Cloudflare composition root without Ent, PostgreSQL, or
Redis. D1 owns only the encrypted secret envelope, enabled timestamp, and
monotonic revision. A user-keyed SQLite Durable Object serializes setup/login
challenges, failed-attempt lockout, and step-up grants; it stores token and JWT
session hashes rather than reusable plaintext. The private route additionally
requires the Container identity installed by the Worker outbound handler.

The local composed-HTTP gate used fresh migrations `0001` through `0006`, a
synthetic password, and local-only secrets. It verified password rejection,
setup/enable, a password login that returned only a 2FA challenge, invalid-code
rejection, successful JWT issuance, challenge replay rejection, session-bound
step-up, password-gated disable, and direct login after disable. D1 readback at
the enabled point found a purpose-prefixed AES-GCM envelope and revision 1; DO
readback found only hashed/opaque transient state. Final readback found revision
2, no envelope, and no setup/login/grant/attempt rows. This was not a visual
browser, remote Cloudflare, real-upstream, or production gate.

# Administrator role changes (Cloudflare mode)

Role promotion and demotion retain the existing
`PUT /api/v1/admin/users/:id` public shape, but only a real administrator JWT
session can enter the privileged path. The Go boundary requires enabled TOTP,
a non-expired step-up grant for that exact JWT session, a normalized
`Idempotency-Key`, and the explicit browser retry marker. Administrator API
keys cannot change roles. Ordinary same-role profile updates remain on the
non-step-up field-patch path.

The private Worker independently checks the actor, target, current session
grant, exact field allowlist, and operation fingerprint. D1 conditionally
commits the user patch, one management-operation result, and one immutable
`admin_role_change_audit` row. The operation fingerprint covers actor, target,
new role, every supplied field, and an operation-salted password semantic
digest; it excludes plaintext, JWT/session values, and the randomized bcrypt
hash. A changed payload under the same key returns
`IDEMPOTENCY_CONFLICT`. A demotion or disable that would remove the final
live administrator returns `LAST_ADMIN_REQUIRED`, including concurrent
self-removal attempts.

The local composed-HTTP gate used a fresh canonical `0001` through `0007`
D1 chain and the real Worker -> Go Container -> private Worker path. It
verified promotion, exact replay, changed-payload conflict, password-bearing
demotion and replay, changed-password conflict, and final-administrator
rejection. Persisted D1 had exactly two immutable audit rows, one successful
operation per role transition, no password/digest/session marker in stored
responses, one remaining live administrator, and no foreign-key violation.
This was not a visual browser, remote Cloudflare, real-upstream, or production
gate.
